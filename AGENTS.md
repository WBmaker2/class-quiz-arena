# 프로젝트 메모리

## 업데이트 내역 누적 기록 (필수)

- 앱·코드·화면에 수정이 생기면 작업을 마치기 전에 반드시 아래 두 곳에 함께 기록한다.
  1. `docs/UPDATELOG.md` — 사람이 읽는 원본. 맨 위에 오늘 날짜 `## YYYY-MM-DD` 섹션을 추가하고(같은 날짜 섹션이 있으면 덧붙임) 쉬운 말로 쓴다.
  2. `src/data/updatelog.ts` — 선생님 화면 ‘업데이트 내역’ 팝업에 보여주는 데이터. md와 같은 날짜·같은 문구로 넣는다(최신 날짜가 맨 앞).
- `src/data/updatelog.test.ts`가 두 파일의 날짜·문구 일치를 검사하므로, 한쪽만 고치면 테스트가 실패한다.
- ‘관리자 모드’ 관련 내용은 업데이트 내역에 쓰지 않는다.
- 커밋·푸시·배포 요청을 받으면 업데이트 내역 변경분을 반드시 함께 포함한다.
